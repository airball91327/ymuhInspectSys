@model IEnumerable<InspectSystem.Models.InspectDocFlow>

@Scripts.Render("~/bundles//jquery")
<link rel="stylesheet" href="~/Content/MobileStyle/MyMobileTabStyle.css" />

@{
    ViewBag.Title = "流程資訊";
}


<h3>流程資訊</h3>
<hr />
<div class="container">
    <a class="btn btn-primary btn-block btn-lg" href="@Url.Action("Index", new { area = "Mobile", docID = Model.First().DocID})">回項目選擇</a>
</div>
<hr />

@foreach (var item in Model)
{
    <div class="tab row">
        <button class="col-xs-12">

            <div>
                <h4>
                    @Html.DisplayNameFor(model => model.StepID)
                    @Html.DisplayFor(modelItem => item.StepID)
                </h4>
            </div>

            <div class="col-xs-12">
                <span class="col-xs-4">@Html.DisplayNameFor(model => model.InspectFlowStatusTable.FlowStatusName)</span>
                <span class="col-xs-8">@Html.DisplayFor(model => item.InspectFlowStatusTable.FlowStatusName)</span>
            </div>

            <div class="col-xs-12">
                <span class="col-xs-4">@Html.DisplayNameFor(model => model.StepOwnerName)</span>
                <span class="col-xs-8">@Html.DisplayFor(modelItem => item.StepOwnerName)</span>
            </div>

            <div class="col-xs-12">
                <span class="col-xs-4">@Html.DisplayNameFor(model => model.Opinions)</span>
                <span class="col-xs-8">@Html.DisplayFor(modelItem => item.Opinions)</span>
            </div>

            <div class="col-xs-12">
                <span class="col-xs-4">@Html.DisplayNameFor(model => model.EditorName)</span>
                <span class="col-xs-8">@Html.DisplayFor(modelItem => item.EditorName)</span>
            </div>

            <div class="col-xs-12">
                <span class="col-xs-4">@Html.DisplayNameFor(model => model.EditTime)</span>
                <span class="col-xs-8">@Html.DisplayFor(modelItem => item.EditTime)</span>
            </div>
        </button>
    </div>
}
